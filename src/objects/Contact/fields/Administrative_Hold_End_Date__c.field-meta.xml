<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Administrative_Hold_End_Date__c</fullName>
    <description>Calculated or manually entered date for Administrative Hold conclusion per Appendix C Section 7 - enforces 90-day maximum unless extended by NSAA</description>
    <externalId>false</externalId>
    <formula>IF(
    NOT(ISBLANK(Administrative_Hold_Start_Date__c)),
    Administrative_Hold_Start_Date__c + 90,
    NULL
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Automatic calculation: 90 days from start date per Appendix C Section 7 maximum period. Extensions require NSAA approval.</inlineHelpText>
    <label>Administrative Hold End Date</label>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Date</type>
</CustomField>
